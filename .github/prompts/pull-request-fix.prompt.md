---
description: "Pull Request Comment Fixer — reads open review threads on a pull request, implements the required code fixes, replies to each thread, and marks resolved threads as fixed."
agent: "pull-request-fixer"
argument-hint: "Pull request ID to fix (e.g. 42). Organization, Project, and Repository default to values in AGENTS.md."
---

Fix the open review comments on this pull request.
