name: AI_Testproject
version: 1.0.0
description: Test project for AI Toolkit
author: Chandra PRAKASH
# Which agent platforms to deploy to.
# Resolution order: --target flag > this field > auto-detect from filesystem.
# Accepted values: agent-skills, antigravity, claude, codex, copilot, cursor, gemini, kiro, opencode, windsurf
targets:
  - copilot
dependencies:
  apm:
    - dev.azure.com/soprasteria-is/ISSSG/AI_Toolkit_For_IS/development-generic#master
  mcp: []
includes: auto
scripts: {}
